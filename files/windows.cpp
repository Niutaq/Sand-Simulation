/*#include "windows.hpp"

Window::Window() {
    box.setSize(sf::Vector2f(50, 50));
    box.setFillColor(sf::Color(100, 0, 0));
    box.setPosition(10, 10);    
    boxID = 3;
}

void Window::DrawWindow(std::vector<std::vector<int>>& grid, sf::RenderWindow& window) {
    window.draw(box);
}

void Window::HandleClickEvent(std::vector<std::vector<int>>& grid, sf::RenderWindow& window, int ID) {

}
*/