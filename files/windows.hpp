/*#pragma once

#include "stuff.hpp"

class Window {
public:
	Window();
    void DrawWindow(std::vector<std::vector<int>>& grid, sf::RenderWindow& window);
    void HandleClickEvent(std::vector<std::vector<int>>& grid, sf::RenderWindow& window, int ID);
private:
	sf::RectangleShape box;
	int boxID;
};
*/